package com.here.c.a.a;

import com.here.services.location.LocationListener;
import com.here.services.location.hybrid.HybridLocationApi.Options;

public interface a extends b {
    boolean a(LocationListener locationListener);

    boolean a(Options options, LocationListener locationListener);
}
